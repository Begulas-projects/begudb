import java.io.IOException;

class index {
    public static void main(String[] args) throws IOException{
        String[][] array = db.injectdb();
        System.out.println(db.find("s"));
        int testing = 1; 
        system.printArrayln(array[db.find(testing + "")]);
    }
}