import java.io.IOException;

class index {
    public static void main(String[] args) throws IOException{
        String[][] array = system.injectdb();
        System.out.println(system.find("s"));
        system.printArrayln(array[system.find("this is a text")]);
    }
}